(()=>{
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const sizeRe=/^(?:XXS|XS|S|M|L|XL|XXL|XXXL|[2-6]XL|[0-9]{1,2}(?:\.[0-9])?|ONE SIZE)$/i;
  const swatches={black:'#171717','vintage black':'#262524','dark grey':'#555654',charcoal:'#41413f',grey:'#777975',gray:'#777975','heather grey':'#9a9b98',white:'#f3f0e9',natural:'#d9ccb6',cream:'#e5dbc7',ivory:'#eee5d3',bone:'#d2c6b2',sand:'#bca98c',tan:'#a98565',brown:'#6e4a35',chocolate:'#55382c',sage:'#89927a','light sage':'#aab39d',olive:'#646a4c','military green':'#4f5840','forest green':'#304b39',green:'#5d7455',navy:'#29374b',blue:'#526d83','light blue':'#91aabd',red:'#8c3d38',maroon:'#653a40',burgundy:'#643640',rust:'#a55e43',orange:'#bf7044',pink:'#c98e91',mauve:'#a8747e','dusty rose':'#b47d7b',purple:'#70566f',lavender:'#a496b2',yellow:'#c6a85b',gold:'#b18a4b'};
  const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n||0));
  const swatch=name=>{const n=String(name||'').toLowerCase().trim();if(swatches[n])return swatches[n];const key=Object.keys(swatches).sort((a,b)=>b.length-a.length).find(k=>n.includes(k));return key?swatches[key]:'#9a8a76'};
  let merchPromise=null;
  const loadMerch=()=>merchPromise||(merchPromise=fetch('/api/merchandising',{headers:{Accept:'application/json'}}).then(r=>r.ok?r.json():{items:[]}).then(d=>new Map((Array.isArray(d.items)?d.items:[]).map(x=>[String(x.productId),x]))).catch(()=>new Map()));
  function parts(v){const a=String(v?.title||'').split(/\s*\/\s*|\s+-\s+/).map(x=>x.trim()).filter(Boolean);if(!a.length)return{color:'Standard',size:'One Size'};if(a.length===1)return sizeRe.test(a[0])?{color:'Standard',size:a[0]}:{color:a[0],size:'One Size'};if(sizeRe.test(a[0]))return{color:a.slice(1).join(' / '),size:a[0]};if(sizeRe.test(a[a.length-1]))return{color:a.slice(0,-1).join(' / '),size:a[a.length-1]};return{color:a[0],size:a.slice(1).join(' / ')}};
  const available=p=>(p?.variants||[]).filter(v=>v.available!==false&&v.is_available!==false&&v.is_enabled!==false).map(v=>({...v,...parts(v)}));
  function imageUrls(p,merchMap){const urls=[];const add=v=>{const src=typeof v==='string'?v:(v?.src||v?.url||v?.image||'');if(src&&!urls.includes(src))urls.push(src)};const local=window.__wildSageMerchandising?.get?.(String(p?.id));for(const x of(local?.mockups||local?.images||[]))add(x);const fetched=merchMap?.get?.(String(p?.id));for(const x of(fetched?.mockups||fetched?.images||[]))add(x);for(const x of(p?.images||[]))add(x);return urls.slice(0,12)}
  function variantImage(p,variantId){return (p?.images||[]).find(i=>(i.variantIds||[]).map(String).includes(String(variantId)))?.src||''}
  function openProductStable(p){
    const dialog=$('#productDialog'),content=$('#productDialogContent');if(!dialog||!content||!p)return;
    const vars=available(p);let color=[...new Set(vars.map(v=>v.color))][0]||'Standard',size='',qty=1,merchMap=new Map();
    content.innerHTML='';
    const grid=document.createElement('div');grid.className='product-dialog-grid';
    const gallery=document.createElement('div');gallery.className='product-gallery';
    const frame=document.createElement('div');frame.className='main-image-frame';
    const main=document.createElement('img');main.id='dialogMainImage';main.alt=p.title||'Product';frame.appendChild(main);gallery.appendChild(frame);
    const thumbs=document.createElement('div');thumbs.className='product-thumbs';gallery.appendChild(thumbs);grid.appendChild(gallery);
    const renderThumbs=()=>{const all=imageUrls(p,merchMap);if(!main.src&&all[0])main.src=all[0];if(!all.length&&main.src)all.push(main.src);thumbs.innerHTML='';all.forEach((src,i)=>{const b=document.createElement('button');b.type='button';b.className='product-thumb'+(main.src===src?' active':'');b.dataset.src=src;b.setAttribute('aria-label',`View ${p.title||'product'} image ${i+1}`);b.style.backgroundImage=`url("${String(src).replace(/"/g,'%22')}")`;const im=document.createElement('img');im.src=src;im.alt=`${p.title||'Product'} mockup ${i+1}`;im.loading='eager';im.decoding='async';im.onerror=()=>b.classList.add('thumb-load-error');b.appendChild(im);b.onclick=()=>{main.src=src;$$('.product-thumb',thumbs).forEach(x=>x.classList.remove('active'));b.classList.add('active')};thumbs.appendChild(b)});};
    renderThumbs();
    loadMerch().then(m=>{merchMap=m;renderThumbs()});
    const copy=document.createElement('div');copy.className='product-dialog-copy';
    const eyebrow=document.createElement('p');eyebrow.className='eyebrow';eyebrow.textContent=(p.tags||[]).slice(0,2).join(' ✦ ')||'WILD SAGE';
    const h=document.createElement('h2');h.textContent=p.title||'Product';
    const price=document.createElement('p');price.className='price';price.textContent=`From ${money(p.minPrice)}`;
    const desc=document.createElement('div');desc.className='product-description';desc.innerHTML=p.description||'';copy.append(eyebrow,h,price,desc);
    if(vars.length){const picker=document.createElement('div');picker.className='variant-picker';picker.innerHTML='<div class="picker-label"><span>Color</span><strong id="chosenColor"></strong></div><div id="colorChoices" class="color-choices"></div><div class="picker-label"><span>Size</span><strong id="chosenSize">Select</strong></div><div id="sizeChoices" class="size-choices"></div>';copy.appendChild(picker);const sticky=document.createElement('div');sticky.className='sticky-add';sticky.innerHTML='<div class="quantity-control"><button id="qtyMinus" type="button">−</button><span id="qtyValue">1</span><button id="qtyPlus" type="button">+</button></div><button id="dialogAdd" type="button" disabled>Add to bag</button>';copy.appendChild(sticky);const note=document.createElement('p');note.className='availability-note';note.textContent='Choose a color first, then select an available size.';copy.appendChild(note);grid.appendChild(copy);content.appendChild(grid);
      const cc=$('#colorChoices',copy),sc=$('#sizeChoices',copy),chosenColor=$('#chosenColor',copy),chosenSize=$('#chosenSize',copy),add=$('#dialogAdd',copy),selected=()=>vars.find(v=>v.color===color&&v.size===size);
      const setActive=src=>$$('.product-thumb',thumbs).forEach(x=>x.classList.toggle('active',x.dataset.src===src));
      const renderSizes=()=>{const sizes=[...new Set(vars.filter(v=>v.color===color).map(v=>v.size))];if(!sizes.includes(size))size='';sc.innerHTML='';sizes.forEach(s=>{const b=document.createElement('button');b.type='button';b.className='size-choice'+(s===size?' active':'');b.textContent=s;b.onclick=()=>{size=s;renderSizes()};sc.appendChild(b)});chosenSize.textContent=size||'Select';const v=selected();add.disabled=!v;if(v){price.textContent=money(v.price);const src=variantImage(p,v.id);if(src){main.src=src;setActive(src)}}};
      const renderColors=()=>{cc.innerHTML='';[...new Set(vars.map(v=>v.color))].forEach(c=>{const b=document.createElement('button');b.type='button';b.className='color-choice'+(c===color?' active':'');b.title=c;b.setAttribute('aria-label',c);const dot=document.createElement('span');dot.style.background=swatch(c);b.appendChild(dot);b.onclick=()=>{color=c;size='';renderColors();renderSizes();const first=vars.find(v=>v.color===color);const src=first?variantImage(p,first.id):'';if(src){main.src=src;setActive(src)}};cc.appendChild(b)});chosenColor.textContent=color};
      renderColors();renderSizes();$('#qtyMinus',copy).onclick=()=>{$('#qtyValue',copy).textContent=qty=Math.max(1,qty-1)};$('#qtyPlus',copy).onclick=()=>{$('#qtyValue',copy).textContent=qty=Math.min(10,qty+1)};add.onclick=()=>{const v=selected();if(!v)return;if(typeof window.addToCart==='function')window.addToCart(p,v,qty);dialog.close();if(typeof window.openBag==='function')window.openBag()};
    }else{const sold=document.createElement('p');sold.className='sold-out';sold.textContent='This piece is currently unavailable.';copy.appendChild(sold);grid.appendChild(copy);content.appendChild(grid)}
    if(!dialog.open)dialog.showModal();
  }
  window.openProduct=openProductStable;window.__wildSageOpenProduct=openProductStable;
  const style=document.createElement('style');style.textContent=`#productDialog .product-thumbs{display:flex!important;visibility:visible!important;opacity:1!important;gap:10px!important;overflow-x:auto!important;flex-wrap:nowrap!important;margin-top:12px!important;padding:4px 2px 8px!important;min-height:78px!important}#productDialog .product-thumb,#productDialog .product-thumb:empty{display:grid!important;visibility:visible!important;opacity:1!important;place-items:center!important;position:relative!important;flex:0 0 68px!important;width:68px!important;min-width:68px!important;height:68px!important;min-height:68px!important;padding:4px!important;border:1px solid rgba(238,231,220,.3)!important;border-radius:5px!important;background:#efe9df center/contain no-repeat!important;overflow:hidden!important}#productDialog .product-thumb img{display:block!important;width:100%!important;height:100%!important;object-fit:contain!important;opacity:1!important;visibility:visible!important;background:#efe9df!important}#productDialog .product-thumb.active{outline:2px solid #eee7dc!important;outline-offset:2px!important}#productDialog .color-choice{display:inline-grid!important;place-items:center!important;width:44px!important;min-width:44px!important;max-width:44px!important;height:44px!important;min-height:44px!important;max-height:44px!important;border-radius:50%!important;padding:5px!important}#productDialog .color-choice span{display:block!important;width:32px!important;height:32px!important;border-radius:50%!important}#productDialog .size-choice{width:auto!important;min-width:50px!important;height:42px!important;border-radius:3px!important;padding:0 13px!important}`;document.head.appendChild(style);
})();