(()=>{
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  let names=new Map(),loaded=false;
  async function load(){try{const r=await fetch('/api/admin/product-names');if(!r.ok)return;const d=await r.json();names=new Map((d.items||[]).map(x=>[String(x.productId),x.displayName||'']));loaded=true;enhance()}catch{}}
  function enhance(){if(!loaded)return;$$('.product-admin-card').forEach(card=>{if(card.querySelector('.product-rename-editor'))return;const id=String(card.dataset.productId||'');if(!id)return;const h=card.querySelector('h2');const original=h?.textContent?.trim()||'';const wrap=document.createElement('div');wrap.className='product-rename-editor';wrap.innerHTML=`<label>Storefront product name<input class="product-display-name" type="text" maxlength="180" placeholder="${original.replace(/"/g,'&quot;')}"></label><div class="rename-actions"><small>Leave blank to use the Printify name.</small><button type="button" class="save-product-name">Save name</button><span class="rename-status"></span></div>`;const input=wrap.querySelector('input');input.value=names.get(id)||'';wrap.querySelector('button').onclick=async()=>{const status=wrap.querySelector('.rename-status');status.textContent='Saving…';try{const r=await fetch(`/api/admin/product-names/${encodeURIComponent(id)}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({displayName:input.value.trim()})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to save');names.set(id,d.item?.displayName||'');if(h)h.textContent=d.item?.displayName||original;status.textContent='Saved';}catch(e){status.textContent=e.message}};const collections=card.querySelector('.product-collections');if(collections)collections.insertAdjacentElement('beforebegin',wrap);else h?.insertAdjacentElement('afterend',wrap)});}
  const style=document.createElement('style');style.textContent=`.product-rename-editor{margin:16px 0;padding:15px;border:1px solid var(--line);background:rgba(139,149,126,.06)}.product-rename-editor label{display:grid;gap:7px;font-size:.72rem;text-transform:uppercase;letter-spacing:.08em}.product-rename-editor input{width:100%;min-height:44px;border:1px solid var(--line);background:#10120e;color:var(--cream);padding:0 12px;font-size:.92rem}.rename-actions{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-top:9px}.rename-actions small{color:var(--muted);margin-right:auto}.save-product-name{padding:9px 13px}.rename-status{font-size:.72rem;color:var(--muted)}`;document.head.appendChild(style);
  const observer=new MutationObserver(()=>enhance());function start(){load();const grid=$('#productsGrid');if(grid)observer.observe(grid,{childList:true,subtree:true})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();
})();